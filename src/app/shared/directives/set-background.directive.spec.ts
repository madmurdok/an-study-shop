import { SetTitleDirective } from './set-background.directive';

describe('SetTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new SetTitleDirective();
    expect(directive).toBeTruthy();
  });
});
